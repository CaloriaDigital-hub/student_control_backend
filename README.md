# 🧠 Student Control

Система для управления учебным процессом — студенты, оценки, расписания, посещаемость.  
Бэкенд построен на **Node.js**, **NestJS** и **Prisma**, база данных — **PostgreSQL**.

---

## 🚀 Установка и запуск

### 1. Установка зависимостей
В корне проекта установите зависимости.  
Можно использовать любой пакетный менеджер (`yarn`, `npm` или `pnpm`).

Пример для **Yarn**:

```bash
yarn install
```


### 2. Создание .env файла

После успешной установки создайте файл .env в корне проекта и добавьте в него базовые переменные окружения:
```
DATABASE_URL="postgresql://<username>:<password>@localhost:5432/student_control?schema=public"
JWT_SECRET="your_jwt_secret"
IS_PUBLIC_KEY="isPublic"
ROLES_KEY="roles"
DB_HOST="localhost"
DB_PORT="5432"
DB_USERNAME="postgres"
DB_PASSWORD="your_password"
DB_NAME="student_control"
NODE_ENV="development"
```


### 3. Создание базы данных

Подключитесь к PostgreSQL и создайте базу данных вручную:
```
CREATE DATABASE student_control;
```

### 4. Миграции Prisma

После создания базы данных выполните миграцию:
```
npx prisma migrate dev --name init


или, если используете Yarn:

yarn prisma migrate dev --name init
```
### 5. Генерация Prisma Client

После миграций сгенерируйте Prisma Client:
```
npx prisma generate
```

или
```
yarn prisma generate
```
### 6. Запуск проекта

После этого можно запустить сервер в режиме разработки:
```
yarn start:dev
```

или для продакшена:
```
yarn start:prod
```
### 🧩 Технологии

Node.js / NestJS

Prisma ORM

PostgreSQL

TypeScript

Yarn / npm / pnpm

### ⚙️ Структура проекта
```
(упрощённая схема)
prisma/
  schema.prisma
src/
 ├── modules/
 ├── core/
 └── main.ts
```

### 📜 Лицензия

Проект распространяется под лицензией MIT.
Разработано с любовью и кофе ☕.
